import data from '../data/DataIPhone.json';
let  initialState = data
const reIPhoneProducts = (state=initialState,action)=>{
    switch (action.type) {
        default: return[...state];
    }
}
export default reIPhoneProducts