import data from '../data/DataHome.json';
let  initialState = data
const reHomeProducts = (state=initialState,action)=>{
    switch (action.type) {
        default: return[...state];
    }
}
export default reHomeProducts